{
  "name": "telegram-anti-spam-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./src/admin.css -o ./public/admin/tailwind.css --minify",
    "dev": "wrangler dev",
    "deploy": "npm run build:css && wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.8.2"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "tailwindcss": "^4.2.4",
    "typescript": "^5.8.3",
    "wrangler": "^4.10.0"
  }
}
